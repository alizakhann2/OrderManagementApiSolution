# OrderManagementApi

ASP.NET Core Web API with Identity + JWT Authentication + SQL Server + EF Core.

Features:
- User registration and login using ASP.NET Core Identity
- JWT-based authentication
- Orders CRUD (ProductName, Quantity, UnitPrice, TotalAmount)
- Each user can manage only their own orders
